/**
 * Testimonials are managed only from the owner admin panel
 * (stored in localStorage via testimonialStore).
 * This file is kept empty on purpose — no hardcoded fallbacks.
 */
export const defaultTestimonials = []

/** @deprecated use testimonialStore */
export const testimonials = defaultTestimonials
